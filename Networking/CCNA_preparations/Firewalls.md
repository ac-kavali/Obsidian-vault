firewall can be place inside the network and outside it , (before reach the router or after reaching it, and in some cases you may have bot at the same time )

### Network firewalls 
are hardware devices that filter trafic between networks 
### Host based firewalls
are software applications that filter traffic entring and exiting a host machine like a pc.